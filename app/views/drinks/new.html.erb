<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>お酒の登録</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_importmap_tags %>
  <!-- ここにBootstrapのCSSリンクまたはその他のスタイルシートのリンクが含まれることを確認 -->
</head>
<body class="d-flex flex-column min-vh-100">
  <header class="mb-5">
    <%= render 'shared/header' %>
  </header>
  <style>
  .form-label, .form-control {
    background-color: rgba(255, 255, 255, 0.8); /* 白色の背景を少し透明に */
    padding: 5px; /* パディングを追加して読みやすく */
    border-radius: 5px; /* 角を丸く */
  }
  .form-control {
    border: 1px solid #ced4da; /* Bootstrapのデフォルトの境界線と似た色 */
  }
</style>

  <div class="container mt-5 mb-5 flex-grow-1"> <!-- コンテンツ領域をflex-growで伸縮させる -->
    <%= form_with model: @drink, local: true, html: { class: "needs-validation", novalidate: true } do |form| %>
      <%= render 'shared/error_messages', object: @drink %>
  
      <div class="container" style="max-width: 500px;"> <!-- フォームの幅を制限し中央寄せ -->
        <div class="form-group">
          <%= form.label :name, "名前", class: "form-label" %>
          <%= form.text_field :name, class: "form-control" %>
        </div>
    
        <div class="form-group">
          <%= form.label :degree, "度数", class: "form-label" %>
          <%= form.number_field :degree, class: "form-control" %>
        </div>
    
        <div class="form-group">
          <%= form.label :volume, "容量", class: "form-label" %>
          <%= form.number_field :volume, class: "form-control" %>
        </div>
    
        <div class="form-group">
          <%= form.label :category_id, "カテゴリ", class: "form-label" %>
          <%= form.collection_select :category_id, Category.all, :id, :name, {}, { class: "form-control" } %>
        </div>
    
        <%= form.hidden_field :predefined, value: false %>
    
        <div class="d-flex justify-content-center mt-4 mb-5">
          <%= form.submit "登録する", class: "btn btn-primary mx-2" %>
          <%= link_to '戻る', top_path, class: "btn btn-secondary mx-2" %>
        </div>
      </div>
    <% end %>
  </div>

  <footer class="bg-info text-white text-center py-3 mt-auto">
    <%= render 'shared/footer' %>
  </footer>
</body>
</html>

  